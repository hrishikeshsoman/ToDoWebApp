import React, { Component, PropTypes } from 'react';

export default class App extends Component {
//all pages goes here through routing

  render() {
    return (
      <div>
        The root component!
      </div>
    );
  }
}
